Tasks:
1. Store OSM auth token somewhere other than the browser cookie
2. Cope with expired tokens and automatically obtaining new ones
3. Introduce caching of OSM results to disk
4. Means of refreshing caches (so curl command can be executed around 4pm)


Have a separate "re-init" process that runs occasionally and obtains the section days/times and stores that locally?
 - Just get the latest programme entry for each section and assume the times are always the same.

Get sections
Get terms
Find all terms that include today's date
For each current term:
    Get programme summary
    
